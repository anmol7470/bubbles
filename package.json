{
  "devDependencies": {
    "mprocs": "^0.7.3",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "install-deps": "bun install && cd client && bun install && cd ../ws && bun install",
    "dev": "mprocs \"cd client && bun run dev\" \"cd ws && bun run index.ts\""
  }
}
